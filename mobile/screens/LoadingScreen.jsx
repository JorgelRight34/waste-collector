import { SafeAreaView, Text, View } from "react-native"

const LoadingScreen = ({ }) => {
    return (
        <SafeAreaView>
            <View>
                <Text>Loading...</Text>
            </View>
        </SafeAreaView>
    )
}

export default LoadingScreen